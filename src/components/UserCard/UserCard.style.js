import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(theme => ({
  userAvatar: {
    width: '3rem',
    height: '3rem',
  },
  userAction: {
    right: 0,
  },
}));
